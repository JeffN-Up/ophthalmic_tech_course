# Copy this file to .env for local development, then replace every placeholder.
# Do not commit real Stripe secret keys or webhook secrets. Launch readiness
# treats placeholders, example.com addresses, and localhost PUBLIC_APP_URL
# values as missing.

# Server-only key used to create Stripe Checkout Sessions.
STRIPE_SECRET_KEY=sk_test_replace_with_your_secret_key

# Optional public key. The current hosted Checkout flow does not require it,
# but keep it here for future Stripe.js/client-side features.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_with_your_publishable_key

# Used by the server to build Stripe success and cancel return URLs.
PUBLIC_APP_URL=http://localhost:3000

# Optional path for `pnpm launch:sitemap`. In production, set PUBLIC_APP_URL to
# the real deployed https domain before generating the sitemap.
LAUNCH_SITEMAP_PATH=dist/public/sitemap.xml

# Final paid-launch switch. Keep false until clinical review, database setup,
# webhook testing, and deployed smoke testing are complete.
ENABLE_PAID_ENROLLMENT=false

# Module 1 clinical signoff. Fill these after the review packet is approved.
MODULE_ONE_CLINICAL_REVIEWER_NAME=
MODULE_ONE_CLINICAL_REVIEWER_ROLE=
MODULE_ONE_CLINICAL_REVIEW_DATE=
MODULE_ONE_CLINICAL_APPROVED_VERSION=
MODULE_ONE_CLINICAL_REVIEW_APPROVED=false

# Managed PostgreSQL connection string for durable purchases and enrollments.
DATABASE_URL=replace_with_managed_postgres_connection_string
DATABASE_SSL=true

# Used by the Stripe webhook receiver to verify events.
STRIPE_WEBHOOK_SECRET=whsec_replace_with_your_webhook_signing_secret

# Optional public Stripe Payment Links for controlled first-buyer checkout while
# automated paid enrollment stays paused. Leave blank until the links are
# approved in Stripe and start with https://buy.stripe.com/.
PUBLIC_STRIPE_PAYMENT_LINK_FOUNDING_LEARNER=
PUBLIC_STRIPE_PAYMENT_LINK_PRACTICE_6_SEATS=
PUBLIC_STRIPE_PAYMENT_LINK_PRACTICE_15_SEATS=

# Used by future passwordless sign-in sessions. Generate a random value at
# least 32 characters long.
AUTH_SESSION_SECRET=replace_with_a_long_random_session_secret

# Used by the transactional email provider for sign-in links.
# Resend works with this direct endpoint.
TRANSACTIONAL_EMAIL_API_URL=https://api.resend.com/emails
TRANSACTIONAL_EMAIL_API_KEY=replace_with_your_resend_api_key
SIGN_IN_FROM_EMAIL="OptiTech Academy <noreply@example.com>"

# Temporary private token for protected practice-seat assignment requests.
# Generate a random value at least 32 characters long.
PRACTICE_SEAT_ADMIN_TOKEN=replace_with_a_long_random_admin_token

# Temporary private token for protected alert-button admin requests.
# Generate a random value at least 32 characters long.
ALERT_ADMIN_TOKEN=replace_with_a_long_random_alert_admin_token

# Optional browser analytics. Leave blank to disable analytics injection.
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
